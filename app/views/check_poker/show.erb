<title>Check a Poker Hand</title>

  <img src="/poker.jpg"></img>
  <%= form_tag('/', method: :post) do %>
    <tr>
      <td><%= text_field_tag "cards", @cards %></td>
    </tr>
    <tr>
      <td><%= submit_tag("Check")%></td>
    </tr>
  <%end %>

  <br>判定結果<br>
<%= @check_result %>

